replicaCount: 3

image:
  repository: nginx
  tag: latest

service:
  type: LoadBalancer
  port: 80

serviceAccount:
  create: true
  annotations: {}
  name: ""

ingress:
  enabled: false

httpRoute:
  enabled: false

autoscaling:
  enabled: false
